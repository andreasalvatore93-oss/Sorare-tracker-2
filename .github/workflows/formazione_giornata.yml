name: Formazione giornata

# Pipeline veloce, basata sulla GIORNATA invece che sui campionati.
#  1) discovery_fixture.py: dalle partite della fixture ricava le squadre in
#     campo, tiene solo le carte possedute di quelle squadre e applica la soglia
#     starter-odds. ~3 minuti invece di ~30, e produce la matrice lega/ruolo.
#  2) predict SOLO per i giocatori sopravvissuti (matrice dinamica: i campionati
#     senza nessun titolare probabile non generano alcun job).
#  3) consigli per lega/ruolo, nello stesso job del predict.
#  4) generatore formazioni.

on:
  workflow_dispatch:
    inputs:
      gameweek:
        description: 'Numero giornata Sorare (es. 95)'
        required: false
        default: ''
      fixture_slug:
        description: 'Slug fixture (es. football-28-31-jul-2026). Ha la precedenza.'
        required: false
        default: ''
      starter_odds_min:
        description: 'Soglia starter-odds (0-1). Odds assenti = escluso.'
        required: false
        default: '0.80'
      allstars:
        description: 'Numero di All Stars da 7'
        required: false
        default: '1'
      arena_allstars_260:
        description: 'Arena All Stars cap 260'
        required: false
        default: '0'
      arena_allstars_220:
        description: 'Arena All Stars cap 220'
        required: false
        default: '0'
      arena_allstars_uncapped:
        description: 'Arena All Stars senza cap'
        required: false
        default: '0'
      in_season:
        description: 'In Season per lega (es. mls:1,kleague:1)'
        required: false
        default: ''
      arena_dedicata:
        description: 'Arene dedicate per lega (es. mls:1,belgio:1)'
        required: false
        default: ''
      list_unused_candidates:
        description: 'Stampa nel log i candidati eleggibili mai schierati (debug, 0/1)'
        required: false
        default: '0'

permissions:
  contents: write

jobs:
  # TEST 28/07 v3 (vedi sezione 30.L del riassunto -- v2 a 6 job confermato
  # funzionante, 30.K): 12 job discovery paralleli. GK e FWD spezzati in 2
  # quote ciascuno, DEF e MID (i piu' affollati) spezzati in 4 -- totale
  # proporzionale alle dimensioni relative dei pool. DISCOVERY_LEAGUE_SHARD
  # ('idx:n') generalizza DISCOVERY_LEAGUE_HALF della v2 a un numero
  # arbitrario di quote, split alfabetico fisso delle leghe di destinazione.
  # Ogni job scrive SOLO le sue leghe (nessuna sovrapposizione reale).
  # discovery_merge combina i 12 MATRICE_JSON parziali. Se il test
  # fallisce/non porta benefici, vedi 30.I per il rollback.
  discovery_gk_0:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      matrice: ${{ steps.run.outputs.matrice }}
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: pip install requests curl_cffi
      - id: run
        env:
          SORARE_COOKIE: ${{ secrets.SORARE_COOKIE }}
          SORARE_CSRF: ${{ secrets.SORARE_CSRF }}
          GAMEWEEK: ${{ github.event.inputs.gameweek }}
          FIXTURE_SLUG: ${{ github.event.inputs.fixture_slug }}
          MIN_STARTER_ODDS: ${{ github.event.inputs.starter_odds_min }}
          DISCOVERY_ROLES: 'gk'
          DISCOVERY_LEAGUE_SHARD: '0:2'
          PYTHONUNBUFFERED: "1"
        run: python discovery_fixture.py
      - if: always()
        run: |
          git config user.name "generatore-formazioni-bot"
          git config user.email "generatore-formazioni-bot@users.noreply.github.com"
          git add formazione_*/output/*_gk_discovery/ || true
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Formazione giornata: slug eleggibili (GK quota 1/2)"
          attempt=0
          until git push; do
            attempt=$((attempt + 1))
            if [ $attempt -ge 15 ]; then exit 1; fi
            sleep $((RANDOM % 12 + 5))
            git fetch origin main
            git merge --no-edit -X ours origin/main
          done
  discovery_gk_1:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      matrice: ${{ steps.run.outputs.matrice }}
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: pip install requests curl_cffi
      - id: run
        env:
          SORARE_COOKIE: ${{ secrets.SORARE_COOKIE }}
          SORARE_CSRF: ${{ secrets.SORARE_CSRF }}
          GAMEWEEK: ${{ github.event.inputs.gameweek }}
          FIXTURE_SLUG: ${{ github.event.inputs.fixture_slug }}
          MIN_STARTER_ODDS: ${{ github.event.inputs.starter_odds_min }}
          DISCOVERY_ROLES: 'gk'
          DISCOVERY_LEAGUE_SHARD: '1:2'
          PYTHONUNBUFFERED: "1"
        run: python discovery_fixture.py
      - if: always()
        run: |
          git config user.name "generatore-formazioni-bot"
          git config user.email "generatore-formazioni-bot@users.noreply.github.com"
          git add formazione_*/output/*_gk_discovery/ || true
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Formazione giornata: slug eleggibili (GK quota 2/2)"
          attempt=0
          until git push; do
            attempt=$((attempt + 1))
            if [ $attempt -ge 15 ]; then exit 1; fi
            sleep $((RANDOM % 12 + 5))
            git fetch origin main
            git merge --no-edit -X ours origin/main
          done
  discovery_def_0:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      matrice: ${{ steps.run.outputs.matrice }}
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: pip install requests curl_cffi
      - id: run
        env:
          SORARE_COOKIE: ${{ secrets.SORARE_COOKIE }}
          SORARE_CSRF: ${{ secrets.SORARE_CSRF }}
          GAMEWEEK: ${{ github.event.inputs.gameweek }}
          FIXTURE_SLUG: ${{ github.event.inputs.fixture_slug }}
          MIN_STARTER_ODDS: ${{ github.event.inputs.starter_odds_min }}
          DISCOVERY_ROLES: 'def'
          DISCOVERY_LEAGUE_SHARD: '0:4'
          PYTHONUNBUFFERED: "1"
        run: python discovery_fixture.py
      - if: always()
        run: |
          git config user.name "generatore-formazioni-bot"
          git config user.email "generatore-formazioni-bot@users.noreply.github.com"
          git add formazione_*/output/*_def_discovery/ || true
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Formazione giornata: slug eleggibili (DEF quota 1/4)"
          attempt=0
          until git push; do
            attempt=$((attempt + 1))
            if [ $attempt -ge 15 ]; then exit 1; fi
            sleep $((RANDOM % 12 + 5))
            git fetch origin main
            git merge --no-edit -X ours origin/main
          done
  discovery_def_1:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      matrice: ${{ steps.run.outputs.matrice }}
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: pip install requests curl_cffi
      - id: run
        env:
          SORARE_COOKIE: ${{ secrets.SORARE_COOKIE }}
          SORARE_CSRF: ${{ secrets.SORARE_CSRF }}
          GAMEWEEK: ${{ github.event.inputs.gameweek }}
          FIXTURE_SLUG: ${{ github.event.inputs.fixture_slug }}
          MIN_STARTER_ODDS: ${{ github.event.inputs.starter_odds_min }}
          DISCOVERY_ROLES: 'def'
          DISCOVERY_LEAGUE_SHARD: '1:4'
          PYTHONUNBUFFERED: "1"
        run: python discovery_fixture.py
      - if: always()
        run: |
          git config user.name "generatore-formazioni-bot"
          git config user.email "generatore-formazioni-bot@users.noreply.github.com"
          git add formazione_*/output/*_def_discovery/ || true
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Formazione giornata: slug eleggibili (DEF quota 2/4)"
          attempt=0
          until git push; do
            attempt=$((attempt + 1))
            if [ $attempt -ge 15 ]; then exit 1; fi
            sleep $((RANDOM % 12 + 5))
            git fetch origin main
            git merge --no-edit -X ours origin/main
          done
  discovery_def_2:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      matrice: ${{ steps.run.outputs.matrice }}
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: pip install requests curl_cffi
      - id: run
        env:
          SORARE_COOKIE: ${{ secrets.SORARE_COOKIE }}
          SORARE_CSRF: ${{ secrets.SORARE_CSRF }}
          GAMEWEEK: ${{ github.event.inputs.gameweek }}
          FIXTURE_SLUG: ${{ github.event.inputs.fixture_slug }}
          MIN_STARTER_ODDS: ${{ github.event.inputs.starter_odds_min }}
          DISCOVERY_ROLES: 'def'
          DISCOVERY_LEAGUE_SHARD: '2:4'
          PYTHONUNBUFFERED: "1"
        run: python discovery_fixture.py
      - if: always()
        run: |
          git config user.name "generatore-formazioni-bot"
          git config user.email "generatore-formazioni-bot@users.noreply.github.com"
          git add formazione_*/output/*_def_discovery/ || true
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Formazione giornata: slug eleggibili (DEF quota 3/4)"
          attempt=0
          until git push; do
            attempt=$((attempt + 1))
            if [ $attempt -ge 15 ]; then exit 1; fi
            sleep $((RANDOM % 12 + 5))
            git fetch origin main
            git merge --no-edit -X ours origin/main
          done
  discovery_def_3:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      matrice: ${{ steps.run.outputs.matrice }}
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: pip install requests curl_cffi
      - id: run
        env:
          SORARE_COOKIE: ${{ secrets.SORARE_COOKIE }}
          SORARE_CSRF: ${{ secrets.SORARE_CSRF }}
          GAMEWEEK: ${{ github.event.inputs.gameweek }}
          FIXTURE_SLUG: ${{ github.event.inputs.fixture_slug }}
          MIN_STARTER_ODDS: ${{ github.event.inputs.starter_odds_min }}
          DISCOVERY_ROLES: 'def'
          DISCOVERY_LEAGUE_SHARD: '3:4'
          PYTHONUNBUFFERED: "1"
        run: python discovery_fixture.py
      - if: always()
        run: |
          git config user.name "generatore-formazioni-bot"
          git config user.email "generatore-formazioni-bot@users.noreply.github.com"
          git add formazione_*/output/*_def_discovery/ || true
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Formazione giornata: slug eleggibili (DEF quota 4/4)"
          attempt=0
          until git push; do
            attempt=$((attempt + 1))
            if [ $attempt -ge 15 ]; then exit 1; fi
            sleep $((RANDOM % 12 + 5))
            git fetch origin main
            git merge --no-edit -X ours origin/main
          done
  discovery_mid_0:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      matrice: ${{ steps.run.outputs.matrice }}
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: pip install requests curl_cffi
      - id: run
        env:
          SORARE_COOKIE: ${{ secrets.SORARE_COOKIE }}
          SORARE_CSRF: ${{ secrets.SORARE_CSRF }}
          GAMEWEEK: ${{ github.event.inputs.gameweek }}
          FIXTURE_SLUG: ${{ github.event.inputs.fixture_slug }}
          MIN_STARTER_ODDS: ${{ github.event.inputs.starter_odds_min }}
          DISCOVERY_ROLES: 'mid'
          DISCOVERY_LEAGUE_SHARD: '0:4'
          PYTHONUNBUFFERED: "1"
        run: python discovery_fixture.py
      - if: always()
        run: |
          git config user.name "generatore-formazioni-bot"
          git config user.email "generatore-formazioni-bot@users.noreply.github.com"
          git add formazione_*/output/*_mid_discovery/ || true
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Formazione giornata: slug eleggibili (MID quota 1/4)"
          attempt=0
          until git push; do
            attempt=$((attempt + 1))
            if [ $attempt -ge 15 ]; then exit 1; fi
            sleep $((RANDOM % 12 + 5))
            git fetch origin main
            git merge --no-edit -X ours origin/main
          done
  discovery_mid_1:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      matrice: ${{ steps.run.outputs.matrice }}
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: pip install requests curl_cffi
      - id: run
        env:
          SORARE_COOKIE: ${{ secrets.SORARE_COOKIE }}
          SORARE_CSRF: ${{ secrets.SORARE_CSRF }}
          GAMEWEEK: ${{ github.event.inputs.gameweek }}
          FIXTURE_SLUG: ${{ github.event.inputs.fixture_slug }}
          MIN_STARTER_ODDS: ${{ github.event.inputs.starter_odds_min }}
          DISCOVERY_ROLES: 'mid'
          DISCOVERY_LEAGUE_SHARD: '1:4'
          PYTHONUNBUFFERED: "1"
        run: python discovery_fixture.py
      - if: always()
        run: |
          git config user.name "generatore-formazioni-bot"
          git config user.email "generatore-formazioni-bot@users.noreply.github.com"
          git add formazione_*/output/*_mid_discovery/ || true
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Formazione giornata: slug eleggibili (MID quota 2/4)"
          attempt=0
          until git push; do
            attempt=$((attempt + 1))
            if [ $attempt -ge 15 ]; then exit 1; fi
            sleep $((RANDOM % 12 + 5))
            git fetch origin main
            git merge --no-edit -X ours origin/main
          done
  discovery_mid_2:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      matrice: ${{ steps.run.outputs.matrice }}
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: pip install requests curl_cffi
      - id: run
        env:
          SORARE_COOKIE: ${{ secrets.SORARE_COOKIE }}
          SORARE_CSRF: ${{ secrets.SORARE_CSRF }}
          GAMEWEEK: ${{ github.event.inputs.gameweek }}
          FIXTURE_SLUG: ${{ github.event.inputs.fixture_slug }}
          MIN_STARTER_ODDS: ${{ github.event.inputs.starter_odds_min }}
          DISCOVERY_ROLES: 'mid'
          DISCOVERY_LEAGUE_SHARD: '2:4'
          PYTHONUNBUFFERED: "1"
        run: python discovery_fixture.py
      - if: always()
        run: |
          git config user.name "generatore-formazioni-bot"
          git config user.email "generatore-formazioni-bot@users.noreply.github.com"
          git add formazione_*/output/*_mid_discovery/ || true
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Formazione giornata: slug eleggibili (MID quota 3/4)"
          attempt=0
          until git push; do
            attempt=$((attempt + 1))
            if [ $attempt -ge 15 ]; then exit 1; fi
            sleep $((RANDOM % 12 + 5))
            git fetch origin main
            git merge --no-edit -X ours origin/main
          done
  discovery_mid_3:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      matrice: ${{ steps.run.outputs.matrice }}
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: pip install requests curl_cffi
      - id: run
        env:
          SORARE_COOKIE: ${{ secrets.SORARE_COOKIE }}
          SORARE_CSRF: ${{ secrets.SORARE_CSRF }}
          GAMEWEEK: ${{ github.event.inputs.gameweek }}
          FIXTURE_SLUG: ${{ github.event.inputs.fixture_slug }}
          MIN_STARTER_ODDS: ${{ github.event.inputs.starter_odds_min }}
          DISCOVERY_ROLES: 'mid'
          DISCOVERY_LEAGUE_SHARD: '3:4'
          PYTHONUNBUFFERED: "1"
        run: python discovery_fixture.py
      - if: always()
        run: |
          git config user.name "generatore-formazioni-bot"
          git config user.email "generatore-formazioni-bot@users.noreply.github.com"
          git add formazione_*/output/*_mid_discovery/ || true
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Formazione giornata: slug eleggibili (MID quota 4/4)"
          attempt=0
          until git push; do
            attempt=$((attempt + 1))
            if [ $attempt -ge 15 ]; then exit 1; fi
            sleep $((RANDOM % 12 + 5))
            git fetch origin main
            git merge --no-edit -X ours origin/main
          done
  discovery_fwd_0:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      matrice: ${{ steps.run.outputs.matrice }}
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: pip install requests curl_cffi
      - id: run
        env:
          SORARE_COOKIE: ${{ secrets.SORARE_COOKIE }}
          SORARE_CSRF: ${{ secrets.SORARE_CSRF }}
          GAMEWEEK: ${{ github.event.inputs.gameweek }}
          FIXTURE_SLUG: ${{ github.event.inputs.fixture_slug }}
          MIN_STARTER_ODDS: ${{ github.event.inputs.starter_odds_min }}
          DISCOVERY_ROLES: 'fwd'
          DISCOVERY_LEAGUE_SHARD: '0:2'
          PYTHONUNBUFFERED: "1"
        run: python discovery_fixture.py
      - if: always()
        run: |
          git config user.name "generatore-formazioni-bot"
          git config user.email "generatore-formazioni-bot@users.noreply.github.com"
          git add formazione_*/output/*_fwd_discovery/ || true
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Formazione giornata: slug eleggibili (FWD quota 1/2)"
          attempt=0
          until git push; do
            attempt=$((attempt + 1))
            if [ $attempt -ge 15 ]; then exit 1; fi
            sleep $((RANDOM % 12 + 5))
            git fetch origin main
            git merge --no-edit -X ours origin/main
          done
  discovery_fwd_1:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      matrice: ${{ steps.run.outputs.matrice }}
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: pip install requests curl_cffi
      - id: run
        env:
          SORARE_COOKIE: ${{ secrets.SORARE_COOKIE }}
          SORARE_CSRF: ${{ secrets.SORARE_CSRF }}
          GAMEWEEK: ${{ github.event.inputs.gameweek }}
          FIXTURE_SLUG: ${{ github.event.inputs.fixture_slug }}
          MIN_STARTER_ODDS: ${{ github.event.inputs.starter_odds_min }}
          DISCOVERY_ROLES: 'fwd'
          DISCOVERY_LEAGUE_SHARD: '1:2'
          PYTHONUNBUFFERED: "1"
        run: python discovery_fixture.py
      - if: always()
        run: |
          git config user.name "generatore-formazioni-bot"
          git config user.email "generatore-formazioni-bot@users.noreply.github.com"
          git add formazione_*/output/*_fwd_discovery/ || true
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Formazione giornata: slug eleggibili (FWD quota 2/2)"
          attempt=0
          until git push; do
            attempt=$((attempt + 1))
            if [ $attempt -ge 15 ]; then exit 1; fi
            sleep $((RANDOM % 12 + 5))
            git fetch origin main
            git merge --no-edit -X ours origin/main
          done
  discovery_merge:
    needs: [discovery_gk_0, discovery_gk_1, discovery_def_0, discovery_def_1, discovery_def_2, discovery_def_3, discovery_mid_0, discovery_mid_1, discovery_mid_2, discovery_mid_3, discovery_fwd_0, discovery_fwd_1]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      matrice: ${{ steps.merge.outputs.matrice }}
    env:
      MATRICE_0: ${{ needs.discovery_gk_0.outputs.matrice }}
      MATRICE_1: ${{ needs.discovery_gk_1.outputs.matrice }}
      MATRICE_2: ${{ needs.discovery_def_0.outputs.matrice }}
      MATRICE_3: ${{ needs.discovery_def_1.outputs.matrice }}
      MATRICE_4: ${{ needs.discovery_def_2.outputs.matrice }}
      MATRICE_5: ${{ needs.discovery_def_3.outputs.matrice }}
      MATRICE_6: ${{ needs.discovery_mid_0.outputs.matrice }}
      MATRICE_7: ${{ needs.discovery_mid_1.outputs.matrice }}
      MATRICE_8: ${{ needs.discovery_mid_2.outputs.matrice }}
      MATRICE_9: ${{ needs.discovery_mid_3.outputs.matrice }}
      MATRICE_10: ${{ needs.discovery_fwd_0.outputs.matrice }}
      MATRICE_11: ${{ needs.discovery_fwd_1.outputs.matrice }}
    steps:
      - id: merge
        run: |
          python3 -c "
          import json, os
          names = ['MATRICE_0','MATRICE_1','MATRICE_2','MATRICE_3','MATRICE_4','MATRICE_5','MATRICE_6','MATRICE_7','MATRICE_8','MATRICE_9','MATRICE_10','MATRICE_11']
          parts = [json.loads(os.environ.get(v) or '[]') for v in names]
          combined = [x for p in parts for x in p]
          print('matrice=' + json.dumps(combined, separators=(',', ':')))
          " >> "$GITHUB_OUTPUT"

  predict:
    needs: discovery_merge
    if: needs.discovery_merge.outputs.matrice != '' && needs.discovery_merge.outputs.matrice != '[]'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    strategy:
      fail-fast: false
      max-parallel: 14
      matrix:
        combo: ${{ fromJson(needs.discovery_merge.outputs.matrice) }}
    env:
      LEGA: ${{ matrix.combo.league }}
      RUOLO: ${{ matrix.combo.role }}
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: pip install requests curl_cffi
      - name: Predict e consiglio
        env:
          SORARE_COOKIE: ${{ secrets.SORARE_COOKIE }}
          SORARE_CSRF: ${{ secrets.SORARE_CSRF }}
          PYTHONUNBUFFERED: "1"
        run: |
          set -u
          case "$RUOLO" in
            gk)  PY=test_gk.py;          CO=build_consiglio_gk.py ;;
            def) PY=test_def.py;         CO=build_consiglio_def.py ;;
            mid) PY=test_mid.py;         CO=build_consiglio_mid.py ;;
            fwd) PY=test_mls_fwd_all.py; CO=build_consiglio.py ;;
          esac
          FILE="formazione_$LEGA/output/${LEGA}_${RUOLO}_discovery/player_slugs.json"
          SLUGS=$(python -c "import json,sys; print(' '.join(json.load(open(sys.argv[1]))))" "$FILE")
          echo "Giocatori da elaborare: $SLUGS"
          for SLUG in $SLUGS; do
            echo "--- $LEGA/$RUOLO: $SLUG ---"
            TARGET_SLUG="$SLUG" python "formazione_$LEGA/predict/$PY" || echo "predict fallito per $SLUG"
          done
          python "formazione_$LEGA/consiglio/$CO" || echo "consiglio non generato"
      - if: always()
        run: |
          git config user.name "generatore-formazioni-bot"
          git config user.email "generatore-formazioni-bot@users.noreply.github.com"
          git add "formazione_$LEGA/output/" || true
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Formazione giornata: $LEGA $RUOLO"
          attempt=0
          until git push; do
            attempt=$((attempt + 1))
            if [ $attempt -ge 15 ]; then exit 1; fi
            sleep $((RANDOM % 12 + 5))
            git fetch origin main
            git merge --no-edit -X ours origin/main
          done

  formazione:
    needs: [discovery_merge, predict]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: pip install requests curl_cffi
      - env:
          SORARE_COOKIE: ${{ secrets.SORARE_COOKIE }}
          IN_SEASON: ${{ github.event.inputs.in_season }}
          ARENA_DEDICATA: ${{ github.event.inputs.arena_dedicata }}
          ARENA_ALLSTARS_260: ${{ github.event.inputs.arena_allstars_260 }}
          ARENA_ALLSTARS_220: ${{ github.event.inputs.arena_allstars_220 }}
          ARENA_ALLSTARS_UNCAPPED: ${{ github.event.inputs.arena_allstars_uncapped }}
          ALLSTARS: ${{ github.event.inputs.allstars }}
          LIST_UNUSED_CANDIDATES: ${{ github.event.inputs.list_unused_candidates }}
          MATCH_WINDOW_DAYS: '7'
          PYTHONUNBUFFERED: "1"
        run: python generatore_formazioni/build_formazione_globale.py
      - if: always()
        run: |
          git config user.name "generatore-formazioni-bot"
          git config user.email "generatore-formazioni-bot@users.noreply.github.com"
          git add generatore_formazioni/output/*.html || true
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Formazione giornata: formazioni generate"
          attempt=0
          until git push; do
            attempt=$((attempt + 1))
            if [ $attempt -ge 15 ]; then exit 1; fi
            sleep $((RANDOM % 12 + 5))
            git fetch origin main
            git merge --no-edit -X ours origin/main
          done
