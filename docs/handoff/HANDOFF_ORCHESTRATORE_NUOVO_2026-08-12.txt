HANDOFF PER IL NUOVO ORCHESTRATORE — si riparte dal PORTIERE
Scritto 11/08/2026 notte (Roma), sessione precedente, per la sessione di
domani (12/08). Leggi questo file per intero prima di fare qualunque cosa.
NON leggere altri handoff per "prendere contesto" oltre a quelli citati
qui: questo file è pensato per essere autosufficiente. L'utente ha già
confermato l'ordine sotto ("lo stregone ha sempre ragione", 11/08 notte).

============================================================================
0. COME SIAMO ARRIVATI QUI (5 righe)
============================================================================

Il filone grade (il voto A-F sulle carte) si è chiuso l'11/08: funziona
ed è già pesato giusto in produzione, nessuna modifica al codice
(`docs/HANDOFF_UNIFICATO_MODELLO_SCOUTING.md` §8bis). Mentre si misuravano
pattern nuovi sugli stessi dati (`archivio_ufficiale/aggregato/
binario2_pool_rows.json`, 10.035 righe carta, zero query nuove), Opus ha
trovato una cosa grossa non cercata: **il modello non prevede i
portieri**. È diventata la priorità, sostituendo il piano originale
(la soglia arena in punti, ora in terza posizione — vedi §3).

============================================================================
1. LA TUA PRIORITÀ: IL PORTIERE — il modello non lo prevede
============================================================================

**Il fatto, misurato su 1.932 portieri reali**: la correlazione fra quanto
il modello si aspettava e quanto il portiere ha fatto davvero è **-0,0018**
(IC95 ±0,045 — lo zero è dentro il margine da ogni lato: non è debole, è
assente). Per confronto: DEF +0,135, MID +0,194, FWD +0,189 — gli altri
tre ruoli un minimo ci azzeccano, il portiere no. Il grade non lo salva
(-0,0232 con grade acceso).

**Perché, è nel codice, non un'ipotesi**
(`generatore_formazioni/build_formazione_globale.py` righe 395-418,
`CALIB_PER_RUOLO`):
  GK : a=35,78  b=0,264
  DEF: a= 7,28  b=0,831
  MID: a=11,61  b=0,740
  FWD: a= 8,40  b=0,789
La pendenza (b) del portiere è un terzo di quella degli altri ruoli:
l'atteso calibrato dei portieri oscilla solo fra 45,5 e 51,7 (dispersione
0,97), mentre il loro punteggio VERO ha dispersione 18,72 — identica a
quella degli altri ruoli. **La calibrazione NON è il bug**: una pendenza
così bassa è la reazione corretta a una previsione grezza senza segnale.
Il problema sta A MONTE, nella previsione grezza del portiere — non
toccare la costante 0,264, peggiorerebbe soltanto.

**Effetto pratico misurato** (dentro pool con ≥3 carte dello stesso
ruolo, "il modello" prende la migliore secondo l'atteso, "a caso" prende
la media del pool):
  GK : 191 pool | guadagno -0,17 punti (t=-0,14) -> ZERO, indistinguibile dal caso
  DEF: 211 pool | guadagno +5,18 (t=+3,91)
  MID: 227 pool | guadagno +3,42 (t=+3,40)
  FWD: 212 pool | guadagno +4,26 (t=+3,58)

**Quanto vale**: portare il portiere dove stanno gli altri ruoli varrebbe
circa **+4 punti per formazione** — stesso ordine di grandezza di tutto il
filone grade appena chiuso (+3,4 punti/formazione). Ma il grade era una
rifinitura di un pezzo che già funzionava; questo è un pezzo che oggi non
funziona per niente. Onestà di Opus: non è garantito che il portiere sia
portabile a +4 (il clean sheet dipende da 10 compagni, forse è
intrinsecamente meno prevedibile) — quello che è certo è che oggi vale
esattamente zero, e nessuno lo sapeva prima di stanotte.

**Il tuo primo passo, letteralmente il primo comando da lanciare, zero
query, ~10 minuti**: guardare la correlazione della previsione GREZZA del
portiere (PRIMA di `calibra()`) col punteggio realizzato.
  - Se anche la previsione grezza ha correlazione ~0 -> il problema è
    nelle FEATURE della pipeline di predizione GK (leggi `formazione_mls/
    predict/test_gk.py` o l'equivalente per la lega che stai testando —
    stesso pattern in tutte le leghe, `formazione_<lega>/predict/`).
  - Se la previsione grezza HA una correlazione positiva ma sparisce dopo
    `calibra()` -> il problema è nella retta di calibrazione (CALIB_PER_RUOLO
    GK), che va ricalcolata, non la pipeline di predizione.
  Sono due mondi diversi. Non procedere oltre senza aver fatto prima
  questo controllo — è la prima cosa che decide dove guardare.

**Dati per farlo**: `archivio_ufficiale/aggregato/binario2_pool_rows.json`
ha già `_cal` (l'atteso CALIBRATO) e `reale` per ogni carta GK, ma non
l'atteso GREZZO pre-calibrazione — quello va ricalcolato passando per
`P.score_atteso(...)` in `analisi_manager/backtest_arene_previsioni.py`
(stesso modulo che usa `p24_binario2_ga.py`, leggi `res['atteso']` prima
della chiamata a `S21.bfg.calibra()` — vedi `prepara_pool_rows()` in
`analisi_manager/p24_binario2_ga.py:150-160` per dove si inserisce).

**Chi esegue**: la misura del primo passo è meccanica, va bene Sonnet.
Se il primo passo indica "problema nelle feature", la diagnosi della
pipeline GK è più delicata (serve giudizio, non solo esecuzione) — valuta
tu se scalare a un brief più dettagliato o procedere direttamente.

============================================================================
1bis. ESITO PRIMO PASSO (12/08/2026, sessione orchestratore) — CONFERMATO:
   il problema è a monte, nelle feature. Diagnosi iniziale già fatta.
============================================================================

**Primo passo (Sonnet esecutore, sessione "Sonnet esecutore")**: n=1932
portieri (match 1932/1932 su `_cal` ricalcolato, zero mismatch — controllo
di produzione riuscito). `corr(atteso_raw, reale) = -0,0037`,
`corr(_cal, reale) = -0,0018` (coincide con l'handoff). Praticamente
identiche: la calibrazione non c'entra, il buco è a monte. File temporaneo
(non committato): `analisi_manager/dati/gk_atteso_raw_2026-08-12.json`.
Difetto annotato da Sonnet (non toccato): `atteso_raw` viene calcolato in
`prepara_pool_rows()` (riga 160) ma buttato via nella costruzione di
`carta_rows` dentro `processa_fixture()`
(`analisi_manager/p24_binario2_ga.py:223-227`) — non entra mai
nell'aggregato scritto su disco. Va aggiunto se in futuro serve di nuovo
senza ricalcolarlo da zero (non fatto ora, decisione rimandata).

**Diagnosi (stessa sessione orchestratore, letta nel codice
`formazione_mls/predict/test_gk.py`, verificata su n=1932 con script
temporanei in `/tmp`, non committati)**: la previsione grezza del portiere
è la somma di 4 pezzi. Scomposti e misurati singolarmente contro il
punteggio vero:

  a) media storica pesata dei punteggi passati (`media_granulari_pesata`)
     -> corr -0,0076 (zero)
  b) pezzo dalle "decisive rates" (parate/gol subiti, POSITIVE_DECISIVE_STAT
     / NEGATIVE_DECISIVE_STAT del dettaglio Sorare, via Poisson,
     `expected_level_from_rates`) -> corr -0,0520, **negativa**: quando
     questo pezzo dice "bene", il portiere in media fa PEGGIO, non meglio.
     E' il pezzo che pesa di più nella formula (sd 3,25 contro sd della
     media storica, molto più piccola).
  c) fattore trend (`compute_trend_factor`) -> risulta ESATTAMENTE 1,0 su
     tutti e 1932 i casi, cioè per il portiere non fa mai nulla (nessuna
     variabilità misurata). Da capire se voluto o un bug — non toccato,
     solo osservato.
  d) blend "porta inviolata di squadra" (`team_cs_prob`, aggiunto 03/08,
     GK_TEAM_CS_WEIGHT=22/35≈0,629, attivo di default) -> è l'UNICO pezzo
     col segno giusto: corr +0,0524 (debole ma POSITIVO), su 1730/1932 GK
     dove `_pcs_squadra()` riesce a calcolarlo (202/1932 = 10,5% restano
     None per mancanza di storico sufficiente nel modello_partita walk-
     forward, e in quei casi il blend semplicemente non si applica --
     comportamento corretto, non un bug).

  Prova diretta che il blend AIUTA (non è inerte, l'interruttore funziona):
  corr SENZA il blend CS = -0,0452 (peggio), corr CON il blend CS =
  -0,0037 (quasi zero). Il blend tira su, ma il pezzo (b) lo cancella quasi
  del tutto.

**In parole povere**: prova a indovinare chi vince una gara con due
indizi. Un indizio (la squadra dietro di lui, punto d) è debole ma giusto
-- un po' come sapere che uno dei corridori si è allenato di più.
L'altro indizio, quello che pesa di più nella previsione (punto b, le
"decisive rates" prese dal dettaglio Sorare), dice l'esatto CONTRARIO di
quello che poi succede davvero. Sommando i due, quasi si annullano e resta
zero. Il problema non è "il portiere è imprevedibile": e' che il pezzo
principale della sua previsione punta nel verso sbagliato.

**Prossimo passo, non ancora fatto**: capire PERCHE' il pezzo (b) ha
segno invertito. Ipotesi da verificare (non ancora testate): (i) le
categorie POSITIVE_DECISIVE_STAT/NEGATIVE_DECISIVE_STAT nel dettaglio
Sorare per un portiere potrebbero non essere "parate vs gol subiti" come
assunto, o esserlo con pesi/segni che a livello aggregato producono
l'effetto opposto; (ii) `expected_level_from_rates`/Poisson potrebbe avere
un bug di segno o di scala specifico per i portieri (LEVEL_SCORE_POISSON_K_MAX,
`netto_to_level`). Anche il fattore trend fisso a 1,0 (punto c) va capito:
se è un bug (dovrebbe muoversi e non lo fa), potrebbe nascondere altro
segnale.

============================================================================
1ter. RETTIFICA (12/08/2026, Opus Esecutore + verifica orchestratore) —
   IL §1bis SOPRA È SBAGLIATO PER PSEUDO-REPLICAZIONE. Il pezzo (b) NON è
   invertito.
============================================================================

**L'errore, verificato di persona**: le "1932 righe GK" del §1bis non sono
1932 osservazioni indipendenti. Sono 995 coppie (slug, fixture) uniche
(243 portieri distinti), perché lo stesso portiere/giornata compare una
volta per ogni manager che lo schiera (`hyun-woo-cho` compare 10 volte
sulla stessa giornata). Ricalcolando la stessa identica correlazione dopo
deduplica su (slug, fixture): `corr(atteso_raw, reale)` passa da **-0,0037
a +0,0276** (verificato dall'orchestratore su
`analisi_manager/dati/gk_atteso_raw_2026-08-12.json`, Opus aveva trovato
+0,0290 — la piccola differenza è nota e spiegata: l'atteso cambia di
qualche decimo/punto da manager a manager perché il cutoff dipende dal
pool di quel manager). Il segno "invertito" del §1bis era rumore da
campione clusterizzato: l'IC95 vero (ricampionando i 243 PORTIERI, non le
righe) è **[-0,036, +0,096]** — dentro lo zero da entrambi i lati, ma
positivo, non negativo.

**Conferma su campione indipendente** (Opus, cache dettaglio game-log,
walk-forward proprio, 3369 punti, 201 portieri, 31 leghe, NON gli stessi
dati del binario2): `corr(level_atteso, level_reale) = +0,0632`, IC95
[+0,029, +0,097] — **esclude lo zero**, positivo. Il pezzo (b) e' quindi
il MIGLIORE dei due pezzi storici del portiere, non il colpevole.

**Cosa NON era sbagliato nel §1bis**: il quadro complessivo (GK ha
segnale ~zero/debole contro DEF/MID/FWD che ordinano bene) regge. Numeri
per ruolo rifatti bene da Opus (dedup + IC a grappolo sui giocatori):
  GK : 995 coppie/243 giocatori, corr dedup +0,029, IC95 [-0,036,+0,096]
  DEF: 1877 coppie/683 giocatori, corr dedup +0,150, IC95 [+0,107,+0,195]
  MID: 1834 coppie/664 giocatori, corr dedup +0,202, IC95 [+0,155,+0,248]
  FWD: 1615 coppie/535 giocatori, corr dedup +0,206, IC95 [+0,145,+0,255]
DEF/MID/FWD sopravvivono alla deduplica. Il GK resta l'unico ruolo con
l'IC che include lo zero: poco segnale, non segnale rovesciato.

**Perché il fattore trend è fisso a 1,0 (punto c del §1bis) — CHIUSO, non
un bug**: `TREND_INTENSITY = 0.0` per il portiere (`test_gk.py:273`,
verificato), abbassato da 0,7 il 31/07 dopo due misure indipendenti (grid
search: MAE peggiora monotono col trend; portieri in serie di
miglioramento rendono -6,19 pt sotto il loro livello, IC95
[-11,15,-1,22]). Comportamento voluto, non un bug — ma la costante non è
mai stata ritarata dopo il fix delle finestre sovrapposte del 03/08:
scaduta, non urgente, un filone da mezz'ora se e quando serve.

**La domanda strutturale vera (Opus, PASSO 2 proposto, non ancora
eseguito)**: l'89% del punteggio decisivo del portiere è CLEAN SHEET (un
evento di SQUADRA), stimato oggi DUE VOLTE: (b) lo deduce dallo storico
personale del portiere via Poisson, (d, il blend team_cs) ci somma una
correzione a scala fissata a mano (GK_TEAM_CS_POINTS=35, peso 0,629). Test
proposto (nessuna modifica prod): confrontare `corr(pcs_squadra,
level_reale)` con `corr(level_atteso_storico, level_reale)=+0,0632`. Se
`pcs` vince, il termine si ricostruisce da modello_partita e lo storico
personale resta solo un prior per il 10,5% dei casi senza pcs — non è una
ritaratura, è togliere un doppio conteggio.

**Attenzione sul "+4 punti/formazione" del §1 originale**: Opus stima che
NON regga. Dai numeri già misurati, guadagno per unità di correlazione è
~18-38 punti (MID 0,194→+3,42; FWD 0,189→+4,26; DEF 0,135→+5,18). Il GK
oggi è a +0,029 dedup; anche raddoppiandolo a 0,06-0,08 (già ambizioso)
vale **~+0,5/+1,5 punti/formazione**, non +4. Per +4 servirebbe corr
~0,15-0,22 (un portiere che ordina come un centrocampista) — niente di
misurato lo sostiene. Vale comunque farlo (è pulito, toglie un doppio
conteggio), ma va venduto come "+1 punto e un difetto in meno", non "+4".

**Regola generale da questo episodio** (da applicare sempre d'ora in poi
su questo tipo di dati): PRIMA di qualunque correlazione su
`archivio_ufficiale/aggregato/binario2_pool_rows.json` (o simili),
deduplicare per (slug, fixture) — la stessa carta/giornata compare una
volta per manager — e calcolare l'IC ricampionando i GIOCATORI, non le
righe. Non fatto la prima volta (§1bis), causa dell'intero abbaglio.

**Soglie per decidere se il PASSO 2 va in produzione** (Opus, regola
MAE+corr+lift insieme, delta nello stesso run): (a) delta di correlazione
positivo con IC95 del delta che esclude lo zero, ordine di grandezza
minimo sensato +0,03; (b) MAE non peggiora di più di 0,2 punti (oggi
16,31 sul campione indipendente di Opus); (c) lift di selezione sui pool
veri (binario2, GK, ≥3 carte) col segno giusto, ma solo come controllo di
sanità — 191 pool non bastano a decidere da soli (l'errore di misura è di
alcuni punti); (d) solo dopo a+b+c: ricalcolare `CALIB_PER_RUOLO['GK']` e
risalire la catena (soglie arena, poi scouting) — non fermarsi al primo
anello.

Script di Opus non committato: `analisi_manager/_tmp_gk_diagnosi_opus.py`
(rigenera tutto in ~2 minuti, zero rete). Risposta integrale di Opus
disponibile nella sessione "Opus Esecutore" se serve il dettaglio.

============================================================================
2. SECONDA PRIORITÀ: IL CAPITANO — riaprire, il vecchio verdetto non regge
============================================================================

Il vecchio filone capitano (8 ipotesi, tutte "chiuse" —
`docs/HANDOFF_UNIFICATO_MODELLO_SCOUTING.md` §5.3) girava su backtest che
leggevano il punteggio dai file manager CON bonus/xp non tolti (bug D6 in
CLAUDE.md). Rifatto pulito stanotte da Opus sull'archivio corretto (1.145
arene con capitano, punteggi veri per carta, D6 non si applica qui):

  bonus capitano incassato dal modello:     +11,36 punti/arena
  scegliendo il capitano A CASO fra le 5:   +10,67
  scegliendo col SENNO DI POI (il migliore):+15,26
  guadagno vero del modello sul caso:        +0,69 (t=6,91, solido)
  massimo possibile sul caso:                +4,59
  -> il modello prende il 15% di quello che c'è sul piatto

Funziona (non è casuale), ma lascia l'85% del valore potenziale sul
tavolo. Costo per riverificare/migliorare: zero query, dati già in repo.
Dettaglio che lega i due temi: il modello non nomina MAI capitano un
portiere (0 su 1.145), ma il portiere è la carta con più punti in 157
arene su 1.145 (13,7%) — in parte è corretto (non punti forte su chi non
sai prevedere), in parte è lo stesso buco del punto 1.

============================================================================
3. TERZA PRIORITÀ (era la prima nel piano di ieri sera, RETROCESSA da
   Opus stesso): la soglia d'ingresso arena in PUNTI, fuori campione
============================================================================

Perché è stata retrocessa: è una regola di decisione che sta SOPRA il
modello. Tararla mentre il portiere (1 slot su 5) è cieco significa
tararla sul rumore di quello slot, e rifarla daccapo quando il portiere si
sistema — è la catena di produzione di CLAUDE.md letta al contrario (si
tara la soglia DOPO aver sistemato la predizione, non prima). Se preferisci
comunque partire da qui, non è una scelta sbagliata, solo meno efficiente.

**Se decidi di farla comunque, il metodo (NON saltare l'ordine):**
1. Decidi PRIMA di guardare un solo numero come dividere il campione in
   due metà indipendenti (taratura / verifica). Scrivilo e committalo
   PRIMA di guardare i dati — se lo decidi dopo aver visto anche un solo
   numero il test è da buttare (è già successo due volte su questo tema:
   due sweep sullo stesso parametro hanno dato risultati opposti a seconda
   di dettagli del campione).
2. Dati: `archivio_ufficiale/aggregato/binario1_out.json` -> `per_gw` ->
   `risultati`, ha `atteso_A`, `atteso_G`, `soglia_decisione`,
   `punteggio_totale` (il PUNTO REALE — usa questo, non `premio_netto`,
   i premi in essenze sono una lotteria che nasconde il segnale) per
   OGNI formazione pulita. Formula della soglia oggi:
   `decidi_entra()` in `analisi_manager/p23_binario1_mga.py:176-188`.
3. Sulla metà di TARATURA: prova diversi scarti X dalla soglia attuale,
   misura il punteggio reale totale di chi entrerebbe. Trova l'X migliore
   SOLO su questa metà.
4. Sulla metà di VERIFICA (mai guardata): applica quell'X e controlla se
   regge. Se non regge, "la soglia va bene così" è un esito valido.

============================================================================
4. QUARTA PRIORITÀ: correlazioni fra compagni di squadra (portiere-
   difensore, attacco-centrocampo)
============================================================================

Idea valida (sposta la VARIABILITÀ della formazione, non i punti attesi —
conta perché i premi arena sono una lotteria: i 20 premi più grandi su 860
valgono il 75% del valore totale), ma bloccata su due cose:
- manca il campo `squadra` nel dump (`p24_binario2_ga.py::prepara_pool_rows`
  lo calcola già, `res.get('squadra')`, ma non lo scrive in `carta_rows` —
  due campi in più, zero query, 5 minuti).
- finché il portiere è cieco (punto 1), misurare la correlazione
  portiere-difensore vuol dire correlare qualcosa di noto con qualcosa che
  non sai prevedere. Ha senso solo DOPO il punto 1.

============================================================================
5. DA TENERE NEL CASSETTO, non un filone: differenze fra leghe
============================================================================

Stessa misura del punto 1, spaccata per lega invece che per ruolo:
Germania (corr 0,28) e MLS (0,22) ordinano bene; Giappone (0,03) e
Argentina (0,01) non ordinano quasi per niente — stesso tipo di buco del
portiere, ma per lega. Non aprire un filone ora: se dopo il portiere si
cerca "il secondo buco", si guarda qui prima di ripartire da zero.

============================================================================
6. REGOLA CHE VALE PIÙ DELLA CLASSIFICA (Opus, testuale)
============================================================================

"UNO ALLA VOLTA. Se l'handoff di domani ne apre tre, non se ne chiude
nessuno." Non iniziare il punto 2 prima che il punto 1 abbia una risposta
scritta (anche se la risposta è "i dati non bastano" — è comunque una
risposta).

============================================================================
7. COSA NON FARE
============================================================================

- Non riaprire il filone grade (§8bis in HANDOFF_UNIFICATO): chiuso,
  serve un'idea davvero nuova per riaprirlo, non solo altri dati.
- Non estrarre altri manager: strada chiusa da Opus l'11/08 sera.
- Non toccare `CALIB_PER_RUOLO['GK']` (la costante 0,264) finché non sai
  se il problema è a monte (feature) o nella retta — spostarla alla cieca
  peggiora, non migliora.

============================================================================
1quater. PASSO 2 ESEGUITO (12/08/2026, orchestratore) — pcs squadra batte
   nettamente lo storico personale, la somma naive DILUISCE il segnale
============================================================================

Test proposto da Opus nel §1ter: confrontare `corr(pcs_squadra, level_reale)`
con `corr(level_atteso_storico, level_reale)`. Fatto, script
`analisi_manager/_tmp_gk_passo2_pcs_vs_storico.py` (non committato in
produzione, temporaneo — rigenera in ~1 minuto, zero rete). Stessa
metodologia di Opus (walk-forward dalla cache dettaglio, IC95 a grappolo
per portiere, half_life di produzione), estesa per calcolare `squadra`/
`opp_slug`/`cutoff` per ogni partita (maggioranza home/away sulle ultime 5
partite giocate, stessa logica di `_squadra()` in
`backtest_arene_previsioni.py:113-126`) e passarli a
`P._pcs_squadra(ctx)`.

  n = 2898 punti, 195 portieri (347 scartati per pcs None, storico
  insufficiente nel modello_partita walk-forward — stesso comportamento
  già noto, non un bug)

  corr(level_atteso_storico, level_reale) = +0,0583  IC95 [+0,0195,+0,0949]
  corr(pcs_squadra,          level_reale) = +0,1336  IC95 [+0,0900,+0,1764]
  corr(media(z_storico, z_pcs), level_reale) = +0,1171  IC95 [+0,0785,+0,1532]

**Verdetto**: `pcs_squadra` da solo prevede più del DOPPIO di quanto fa
oggi lo storico personale del portiere (0,134 contro 0,058, entrambi con
IC che esclude lo zero). E quando li si combina in modo naive (media
semplice dei due, non è il blend di produzione — vedi limite sotto), il
risultato PEGGIORA rispetto a pcs da solo (0,117 < 0,134): lo storico
personale non aggiunge, diluisce.

**Limite dichiarato**: la "media semplice" qui sopra NON è il blend reale
di produzione (che soma additivamente `team_cs_weight * GK_TEAM_CS_POINTS
* (pcs - baseline)` con pesi fissati a mano, non una media z-standardizzata
50/50). Non è quindi la controprova diretta che il blend attuale sia
sub-ottimale, solo l'indizio qualitativo che pcs merita più peso di quanto
oggi gliene dia lo storico personale nella somma finale — coerente col
fatto già misurato nel §1bis/1ter che SENZA il blend CS la corr era
-0,045 e CON il blend sale (di poco) verso lo zero: il blend c'è già e
aiuta, solo che lo storico personale (il pezzo più a monte, non il blend)
lo trascina indietro.

**Prossimo passo, non fatto**: provare pesi diversi fra `pcs_squadra` e
`level_atteso_storico` (non media 50/50, una griglia, es. 0/100,
25/75, 50/50, 75/25, 100/0) sullo stesso campione walk-forward, e vedere
se un peso più sbilanciato su pcs batte sia pcs-da-solo sia
lo-storico-da-solo — solo allora avrebbe senso proporre una modifica
reale a `compute_score_atteso_gk`. Nessuna modifica al codice di
produzione fatta finora.

============================================================================
8. FILE DI RIFERIMENTO
============================================================================

- `docs/handoff/RISPOSTA_OPUS_PATTERN_NUOVI_2026-08-11.txt` — la risposta
  integrale di Opus, tutti i numeri di questo file vengono da lì.
- `docs/HANDOFF_UNIFICATO_MODELLO_SCOUTING.md` §8bis — stato del grade
  (chiuso), §5.3 — vecchio verdetto capitano (superato dal punto 2 sopra),
  §5.6 — nota già aperta sul GK level_score binario (probabilmente
  collegata al buco di stanotte, da verificare).
- `archivio_ufficiale/aggregato/binario1_out.json`, `binario2_out.json`,
  `binario2_pool_rows.json` — dati grezzi.
- `docs/handoff/HANDOFF_ORCHESTRATORE_BINARIO_GVSA_2026-08-10.txt` —
  cronologia completa del filone grade appena chiuso.
