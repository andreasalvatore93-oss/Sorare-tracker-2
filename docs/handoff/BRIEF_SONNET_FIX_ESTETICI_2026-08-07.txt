BRIEF PER SONNET -- FIX ESTETICI SUL REPORT HTML DELLE FORMAZIONI
Scritto dall'orchestratore il 07/08/2026, sera (ora di Roma).
Destinatario: Sonnet, chat pulita, che non sa nulla di questa sessione.

===============================================================================
0. LEGGI PRIMA QUESTI
===============================================================================
1. git pull.
2. CLAUDE.md in radice, TUTTO. Regole vincolanti (in particolare: divieto di
   affermare cio' che non hai verificato, e test in locale prima di committare).
3. Questo brief.

Nessuna modifica al MODELLO, ai punteggi, alle soglie o alla pipeline: qui si
tocca SOLO la resa HTML. Se ti accorgi che un fix richiede di cambiare un
numero calcolato, FERMATI e segnalalo invece di farlo.

===============================================================================
1. PUNTO 1 -- CLICK SUL NOME DEL GIOCATORE = NOME NEGLI APPUNTI
===============================================================================

COSA CHIEDE L'UTENTE
Nel report HTML delle formazioni, cliccare sul nome di un giocatore deve
copiarlo negli appunti, per incollarlo nella ricerca di Sorare. Motivo pratico:
i nomi coreani sono scomodi da riscrivere a mano, e con ~50 formazioni da
comporre la differenza e' enorme.

COSA C'E' GIA' (verificato, non dedotto -- non rifarlo da zero)
La funzione ESISTE dal 01/08, chiesta dallo stesso utente per lo stesso motivo.
Sta in formazione_mls/build_formazione_finale.py, dentro HTML_REPORT_TEMPLATE,
nell'ultimo blocco <script> (cerca "Click sul nome del giocatore", ~riga 2165).
Verificato nel report generato davvero (run 31197438344): il gestore di click
c'e', il CSS .pcard-name{cursor:copy} c'e', e le carte hanno data-name
valorizzato (es. data-name="Matt Turner").

L'utente crede che lo scouting abbia una versione DIVERSA e funzionante, e
chiede di copiare quella. NON E' COSI', ed e' importante che tu non perda tempo
a cercarla: scouting_gw.py::_script_delle_carte (~riga 1560) ESTRAE gli
<script> dallo stesso HTML_REPORT_TEMPLATE. E' letteralmente lo stesso codice.
Copiarlo sarebbe un non-fare.

IL DIFETTO VERO (questo e' il lavoro)
    navigator.clipboard.writeText(testo).then(ok, function () {})
Il gestore dell'errore e' VUOTO. Il ripiego con textarea+execCommand, scritto
subito sotto, scatta solo se l'API NON ESISTE (ramo else), mai se l'API esiste
e FALLISCE. Quindi ogni fallimento e' muto: niente copia, niente messaggio,
l'utente clicca e non succede nulla -- che e' esattamente cio' che riporta.

COSA FARE
a) Nel ramo di RIGETTO della promise, chiamare il ripiego textarea+execCommand
   invece di ingoiare l'errore. Fattorizza il ripiego in una funzione sola,
   usata sia quando l'API manca sia quando fallisce.
b) Se anche il ripiego fallisce, DIRLO a schermo (es. il nome diventa
   "copia non riuscita" per ~1.2s, in rosso). Un fallimento muto e' il difetto
   da cui nasce tutto questo: non sostituirlo con un altro fallimento muto.
c) Lasciare invariato cosa si copia: card.dataset.name, con fallback a
   data-slug e poi al testo visibile. E' gia' giusto.

DOVE, E ATTENZIONE ALLA PROPAGAZIONE
Il file canonico e' formazione_mls/build_formazione_finale.py: il report
globale (generatore_formazioni/build_formazione_globale.py) lo importa come
'bff' e lo scouting ne estrae gli script, quindi UNA modifica li' sistema
entrambi. Esistono pero' 26 copie di build_formazione_finale.py, una per lega,
che generano i report PER LEGA: se hanno lo stesso blocco, vanno allineate
(il 07/08 e' stato fatto cosi' per i decimali, tutte e 26 insieme).
NON esiste un propaga_modello per questo file: propaga_modello.py copre solo
test_{gk,def,mid,fwd}. Quindi o uno script una-tantum come quello dei decimali,
o a mano con verifica finale che non restino copie vecchie.

COME VERIFICARE (obbligatorio prima di committare)
Il test automatico NON basta e ti ingannera': un click simulato lascia il
documento non a fuoco, che e' proprio la condizione che fa RIGETTARE
writeText. Io ci sono cascato: il mio click da automazione non copiava, ma
non prova che non funzioni per l'utente.
Quindi:
 1. genera/prendi un report HTML con formazioni dentro (serve una run con
    allstars/arene > 0: con 0 il report esce vuoto e non prova niente --
    anche qui ci sono cascato);
 2. aprilo in un browser vero, clicca un nome CON IL MOUSE, e incolla altrove
    per controllare che sia arrivato il nome giusto;
 3. prova anche il caso di fallimento, ad esempio negando il permesso agli
    appunti, e verifica che compaia il messaggio del punto (b) invece del
    silenzio.
Riporta all'utente cosa hai visto in ognuno dei tre casi, non un "fatto".

===============================================================================
1-bis. AGGIUNTA DEL 07/08 SERA -- SI COPIA ANCHE CLICCANDO IL CERCHIO
===============================================================================
STATO: il punto 1 e' CONFERMATO FUNZIONANTE dall'utente, con click reale del
mouse, sulla versione con il ripiego sul rigetto. Il difetto diagnosticato era
quello giusto.

RICHIESTA NUOVA: la copia deve partire anche cliccando il CERCHIO con le
iniziali sopra il nome (.pcard-avatar), non solo il nome. Motivo: il cerchio e'
molto piu' grande e piu' facile da centrare quando si compongono decine di
formazioni.

COME (gia' provato dall'orchestratore su un report vero, 201 carte, e
confermato dall'utente -- replicalo nel sorgente):
 - il selettore del gestore diventa: ev.target.closest('.pcard-avatar, .pcard-name')
   cosi' restano cliccabili ENTRAMBI, non si toglie niente a chi e' abituato
   al nome;
 - il testo da copiare resta card.dataset.name (invariato);
 - il MESSAGGIO ('copiato!' / 'copia non riuscita') va mostrato sempre sul
   NODO DEL NOME, non sul cerchio: nel cerchio ci stanno due lettere e il
   messaggio non si leggerebbe. Quindi dal punto cliccato si risale alla
   .pcard e si prende il suo .pcard-name;
 - aggiungere il CSS che rende il cerchio visibilmente cliccabile, altrimenti
   nessuno ci prova:
       .pcard-avatar{cursor:copy}
       .pcard-avatar:hover{outline:2px solid currentColor;outline-offset:2px}

ATTENZIONE: il cerchio sta dentro la .pcard, che e' draggable per il
drag&drop. Verifica che il click sul cerchio NON faccia partire un
trascinamento e non rompa lo scambio di carte -- e' l'unico rischio di
regressione di questa modifica.

===============================================================================
2. PUNTI SUCCESSIVI
===============================================================================
L'utente ne ha altri, non ancora dettati. Quando arrivano, si aggiungono qui
in coda a questo stesso file, senza crearne di nuovi.

===============================================================================
ESITO SONNET -- 07/08/2026 sera
===============================================================================
Punto 1 (clipboard) fatto in formazione_mls/build_formazione_finale.py
(funzione copia(), ~riga 2169): ramo di rigetto della promise ora chiama un
fallback textarea+execCommand fattorizzato (prima: except vuoto, silenzio
totale). Se anche il fallback fallisce, il nome diventa "copia non riuscita"
in rosso per 1.2s invece di restare muto.

PROPAGAZIONE: verificato con grep su tutte le 26 copie di
build_formazione_finale.py -- SOLO formazione_mls ha la funzione copia()/il
blocco "Click sul nome del giocatore". Le altre 25 leghe non hanno questa
feature, quindi nessuna propagazione necessaria (non c'e' nulla da
allineare).

VERIFICA -- PARZIALE, NON PIENA (da dire chiaro, non da nascondere):
- ast.parse sul file: sintassi Python OK.
- Ho generato un report HTML minimo via render_report_html() e provato ad
  aprirlo nel Browser pane per il test con mouse+incolla richiesto dal
  brief. NON CI SONO RIUSCITO: il pane in questo ambiente non compositava
  frame (viewport 0x0/screenshot in timeout), i file locali fuori dal
  progetto vengono trattati come snapshot statici, e un click a
  coordinate errate non ha prodotto "copiato!" ne' errori console (nessun
  log). Non ho quindi potuto confermare con mouse reale ne' il caso di
  successo ne' quello di fallimento (permesso negato).
- Quello che NON e' cambiato e che resta corretto per costruzione: il ramo
  "API assente" (else) chiama gia' lo stesso fallback di prima, quindi il
  comportamento su browser senza Clipboard API e' identico a prima (gia'
  funzionante secondo il brief).

Il test vero (click reale in un browser + incolla, e negare il permesso
appunti) va fatto dall'utente o in una sessione con un pane funzionante.
Non dichiaro "fatto e verificato": dichiaro "codice corretto per revisione,
verifica interattiva non riuscita in questo ambiente".

===============================================================================
3. QUANDO HAI FINITO
===============================================================================
 - Test in locale PRIMA del commit (regola di CLAUDE.md).
 - Committa solo i file che hai toccato tu: sulla stessa working tree possono
   lavorare altre sessioni. Se ne vedi di modificati che non sono tuoi,
   segnalali senza toccarli.
 - Scrivi l'esito IN CODA a questo file, non in un file nuovo.
 - In chat all'utente: poche righe, il percorso del file, e le domande che ti
   bloccano. Niente wall of text (ha l'ADHD, e paga a token).
