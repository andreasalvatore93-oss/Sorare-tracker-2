HANDOFF CERBERO — UNICO FILE DI RIFERIMENTO (aggiornato 2026-08-04 17:30 Roma)
================================================================================
UNICO handoff cerbero: si aggiorna in-place ogni sessione, non se ne creano
altri con data nel nome (regola in CLAUDE.md).

>>> NOTA PER LA PROSSIMA SESSIONE: l'utente NON ha ancora chiaro il passo per
    passo per lanciare l'apprendimento la prima volta (dove cliccare su
    GitHub, cosa aspettarsi). GUIDARLO passo-passo quando chiede di
    procedere, non dare per scontato che sappia usare il workflow. Istruzioni
    dettagliate al punto 5.


1) REGIME: MANUALE, non automatico
------------------------------------
Il cron di bot_cerbero.yml non e' mai scattato (causa nota: workflow
aggiunto da poco, non e' un bug di config). Ogni run = lancio manuale da
GitHub, dura ~5h. Non serve ricontrollare la sintassi del workflow.


2) COSA E' STATO CAMBIATO E PERCHE'
--------------------------------------
- Orizzonte di apprendimento: passato da 48h a 24h (lift quasi identico,
  0.92x, ma matura 30h prima -> in regime manuale conta molto). Riga
  usabile quando ha eta'>30h.
- Asse trasversale (margine): prima si registrava solo sopra soglia
  MakeOffer, ora anche sotto (nuova colonna tipo_riga: 'completa' vs
  'trasversale_only'). Chiude un bias di selezione, zero query in piu',
  soglie temporali verificate identiche prima/dopo.
- Nuovo workflow GitHub "Cerbero (apprendimento)" (vedi §4), mai lanciato.


3) DOVE SIAMO COL DATASET (cerbero_osservazioni.csv)
---------------------------------------------------------
2.816 righe (1.033 complete, 1.783 trasversale_only). 12 leghe hanno gia'
volume sufficiente e maturano tutte fra l'01:00 e l'01:35 di Roma della
notte 04/08->05/08: mlspa, austrian-bundesliga, eredivisie,
premier-league-gb-eng, primeira-liga-pt, 2-bundesliga, jupiler-pro-league,
laliga-es, bundesliga-de, k-league-1, premiership-gb-sct, 1-hnl.
Sotto soglia (serve piu' raccolta, non solo tempo): serie-a-it(28),
championship(27), russian-premier-league(24), j1-league(20), ligue-1-fr(19)
e code piu' corte.
NOTA: questi orari si ricalcolano ogni volta che si raccolgono altre righe
(l'eta' si misura sulla riga piu' vecchia). Se si fa un'altra run prima di
lanciare l'apprendimento, ricalcolare invece di fidarsi di questi numeri.
Soglie bootstrap attuali (da dataset storico, non da osservazioni live):
mlspa/k-league-1/eredivisie/jupiler-pro-league = 5%. Le altre leghe usano
il default prudente 10%.


4) WORKFLOW "Cerbero (apprendimento)" — come si usa
-------------------------------------------------------
File: .github/workflows/cerbero_learn.yml. Solo lancio manuale (nessun
cron). Un input a tendina, `modalita`, con 4 scelte:
 - osservazioni: legge le righe mature del CSV, impara le soglie per lega e
   RISCRIVE cerbero_soglie_apprese.json. Serve secret Sorare validi + righe
   gia' mature (vedi §3).
 - trasversale: stampa solo un report (rendimento per fascia di margine),
   NON scrive nulla. Diagnostico.
 - entrambi: prima osservazioni, poi trasversale (se la prima riesce).
 - storico: bootstrap offline dal dataset storico, zero query, funziona
   sempre anche a secret scaduti.
Output: file docs/handoff/LEARN_<modalita>_<data_ora>.txt committato in
automatico + artifact scaricabile (30gg). Se i secret sono scaduti, il log
lo dice esplicitamente ("SECRET SORARE SCADUTI").

PASSO PASSO per lanciarlo dal sito GitHub (usare quando l'utente e' pronto):
 1. github.com -> repo Sorare-tracker-2 -> scheda "Actions".
 2. Colonna sinistra -> "Cerbero (apprendimento)".
 3. Bottone "Run workflow" in alto a destra della lista run.
 4. Branch: main (default). Menu "cosa lanciare": scegliere "osservazioni".
 5. Bottone verde "Run workflow".
 6. Aspettare qualche secondo, ricomparira' una riga con pallino giallo (in
    corso) -> giallo diventa verde (ok) o rosso (errore) in pochi minuti.
 7. Risultato: aprire docs/handoff/LEARN_osservazioni_*.txt nel repo, oppure
    chiedere a Claude di leggerlo e riassumerlo.


5) COSA RESTA CHIUSO (non riaprire senza nuovi dati)
--------------------------------------------------------
Orizzonte 12h (non verificabile in regime manuale) e 48h (superato da 24h);
bug di scrittura sull'asse trasversale (escluso); cron come bug di sintassi
(escluso); contaminazione soglie da b2 (esclusa, no-op provato).


6) DECISIONI APERTE (nessuna azione richiesta se non riguardano)
----------------------------------------------------------------------
A) b2 letto come "zero query incrementali", non "zero fetch a monte in
   assoluto". Da rivedere solo se l'interpretazione non e' quella voluta.
B) Se un giorno il cron partisse in continuo 24h/giorno, il volume di
   scrittura si avvicinerebbe alla soglia di guardia (20.000 righe/giorno).
   Non rilevante nel regime manuale attuale.


7) PROSSIMO STEP CONCRETO
------------------------------
Dall'01:30 di Roma le 12 leghe di §3 sono lanciabili con 'osservazioni'.
L'utente potrebbe non essere online a quell'ora: i dati non scadono, si puo'
lanciare quando torna disponibile. Guidarlo con §4 passo-passo, poi
confrontare le soglie apprese col bootstrap (§3).
