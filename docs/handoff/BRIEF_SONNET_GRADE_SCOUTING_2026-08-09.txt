BRIEF PER SONNET -- GRADE NELLO SCOUTING + CACHE PREDICT
Scritto dall'orchestratore, 09/08/2026 notte (Roma, CEST).

Chat pulita: non sai nulla di questo repo. Leggi PRIMA, in quest'ordine:
  1. CLAUDE.md (regole di lavoro; in cima c'e' la regola nuova sui backtest)
  2. docs/HANDOFF_UNIFICATO_MODELLO_SCOUTING.md, sezioni 2.2 (scouting),
     8bis (grade G), 8ter (scouting dopo il grade), 8undecies (come si
     parla con l'API Sorare: i TRE pezzi obbligatori per l'autenticazione)
Il codice vince sui documenti: se divergono, segui il codice e correggi
il documento nello stesso commit.

--- PERCHE' ESISTE QUESTO LAVORO -----------------------------------------

Il generatore di formazioni usa il grade (la lettera A..F che Sorare
pubblica prima della partita) dal 07/08. Lo scouting acquisti NO:
verificato il 09/08 sul codice, `scouting_gw.py` non contiene nessuna
occorrenza della parola "grade", e la tabella HTML non ha nessuna colonna
per la lettera (le colonne sono Conviene/Giocatore/R/Atteso/Prezzo/
Ess/GW/Si ripaga in/Odds/Note, riga ~1702).

L'utente vuole ALLINEARE i due strumenti. Non e' una dimenticanza da
riparare in fretta: e' strutturale, e va capito prima di scrivere codice.

COME IL GENERATORE PRENDE IL GRADE (letto in discovery_fixture.py:364-479):
`fetch_grade_live(fixture_slug)` pagina il BENCH (`myFilteredBench`) su 3
leaderboard x 4 posizioni e legge `eligiblePlayerGameScores.projection.
grade`. E' bulk, ma il bench contiene SOLO LE CARTE POSSEDUTE, e la query
richiede sessione autenticata. In produzione la fetch la fa un job unico
che passa il risultato agli altri come artifact `pool_gw.json`.

PERCHE' NON SI PUO' COPIARE NELLO SCOUTING: lo scouting valuta carte NON
possedute (e' tutto il suo scopo, §2.2). Nel bench non ci sono. Quindi la
strada del generatore, per costruzione, non le vede.

--- COSA DEVI FARE: TRE COMPITI, IN QUEST'ORDINE --------------------------

=== COMPITO 1 (MISURA, nessuna modifica al codice) ===
DOMANDA: la query `searchPlayers` che lo scouting gia' fa puo' portare
anche il grade, senza nessuna richiesta in piu'?

Contesto: `scouting_gw.py` chiama `searchPlayers` paginata (~12 chiamate
per l'intera giornata, costante `SEARCH_QUERY`, funzione a riga ~493) e da
li' ottiene gia' L5/L10/L40, presenze, infortuni, prezzo minimo, starter
odds e `nextClassicFixtureProjectedScore`. Se lo stesso `anyPlayer` dentro
`commonPlayerHits` accetta anche il campo della lettera, il grade arriva
GRATIS nelle stesse chiamate.

Come si prova (l'introspezione GraphQL e' DISABILITATA su Sorare, vedi
CLAUDE.md: non tentare `__type`/`__schema`, si legge il messaggio d'errore
di una query mirata):
 a) parti dalla `SEARCH_QUERY` esistente, NON riscriverla da zero;
 b) aggiungi sull'`anyPlayer` il pezzo `playerGameScores(last: 3) {
    projection { grade } }` (e' la rotta gia' usata e verificata
    funzionante su giocatori NON posseduti, vedi
    analisi_manager/raccolta_grade_storico.py e §8bis);
 c) lancia UNA sola pagina (page 1, pageSize piccolo) e riporta la
    risposta esatta: accettata? errore di validazione? errore di
    complessita' ("depth N / complexity M")?
 d) se passa: quante righe su quella pagina hanno effettivamente una
    lettera non nulla, e per quale partita e' quella lettera (la
    proiezione e' PER PARTITA, non per giocatore -- §8bis). Serve sapere
    se e' la partita della giornata che lo scouting sta valutando.

ATTENZIONE ALLE TRAPPOLE GIA' PAGATE:
 - il CLIENT conta: la stessa query passa col client di
   `scanners/bot_profit.py` e viene respinta per complessita' con altri
   (trappola 6 in §8, e commento a scouting_gw.py:~515). Usa lo stesso
   client che usa gia' lo scouting (`_client_ritmato()`).
 - i nomi leggibili in `searchPlayers` sono ALIAS (trappola 5).
 - se aggiungendo il campo la query inizia a farsi respingere per
   complessita' dalla pagina 2 in poi, dillo: e' esattamente il modo in
   cui questo errore si e' gia' presentato una volta.

SE LA RISPOSTA E' NO (query respinta): fermati e riportalo. L'alternativa
sarebbe una query per giocatore (centinaia), e se costa tanto la decisione
se valga la pena la prende l'utente, non tu.

SE LA RISPOSTA E' SI': NON implementare ancora l'uso del grade nel
punteggio. Riporta e basta. Come il grade debba PESARE su una decisione
d'acquisto pluri-giornata (mentre la lettera vale per una sola partita) e'
una decisione aperta dell'utente, §8ter -- non una scelta tecnica tua.

=== COMPITO 2 (VERIFICA, poi eventuale modifica) ===
CACHE PREZZI. L'utente ricordava di averla, e HA RAGIONE: esiste in
`best_five.py` (`PREZZI_CACHE_PATH = best_five_prezzi_cache.json`, TTL
`BEST_FIVE_PREZZI_CACHE_TTL_ORE` default 5 GIORNI, alzato da 24h il 31/07
su sua richiesta; c'e' anche una cache L10 con lo stesso schema).

MA -- e questo va verificato da te, non dato per buono: nello SCOUTING i
prezzi NON sembrano costare query aggiuntive. `_prezzo_eur(p)` legge
`lowestPriceAnyCard` dal payload di `searchPlayers`, cioe' arriva dentro
le stesse ~12 chiamate. Se e' cosi, aggiungere una cache prezzi allo
scouting non risparmierebbe NIENTE e in cambio darebbe prezzi vecchi su
una decisione d'acquisto.

Quindi: verifica sul codice se lo scouting fa QUALCHE query dedicata ai
prezzi (cerca `liveSingleSaleOffers`, `lowestPriceAnyCard`, e ogni
chiamata dentro il percorso dei prezzi). Riporta il conteggio vero delle
query per prezzi in una run di scouting.
 - Se sono ZERO query dedicate: NON implementare nessuna cache, spiegalo.
 - Se invece ci sono query dedicate: allora la cache serve davvero, e
   l'utente la vuole con blocco di 2-3 GIORNI ("una volta cachato un
   prezzo, tienilo, non si sposta di molto"). In quel caso riusa lo schema
   gia' esistente in best_five (stesso file-schema, stesso meccanismo di
   TTL), NON scriverne uno nuovo.

=== COMPITO 3 (MODIFICA, richiesta esplicita dell'utente) ===
DISATTIVARE IL RIUSO DELLE PREDIZIONI nello scouting.

Motivo dell'utente: finche' G non e' innestato con sicurezza, non vuole
che lo scouting riusi previsioni gia' su disco -- una previsione salvata
prima potrebbe essere stata calcolata senza G o con un grade diverso, e
verrebbe riusata in silenzio.

Stato verificato: il flag esiste NEL CODICE
(`best_five.RIUSA_PREDIZIONI`, legge `BEST_FIVE_RIUSA_PREDIZIONI`,
default '1', best_five.py:482; lo scouting lo usa via
`bf._predizione_riutilizzabile(...)`, scouting_gw.py:~1081) ma **NON e'
esposto da nessuna parte all'utente**: nella schermata "Run workflow" di
`scouting_gw.yml` gli unici controlli sono numero di giornata, soglia
starter-odds, "far girare il modello sui candidati" e "controllo 2-su-3".
Verificato dall'utente sullo screenshot della UI, 09/08. Quindi oggi lui
NON ha modo di spegnere il riuso senza modificare il codice: e' questo il
difetto da chiudere, non solo il valore del default.

Fai due cose:
 a) **spegnilo di default per lo scouting** (l'utente lo vuole spento
    finche' G non e' innestato con sicurezza). Non spegnerlo globalmente
    per chiunque importi best_five: NON toccare il riuso nel generatore
    di formazioni, qui si parla solo dello scouting;
 b) **esponilo come input del workflow** `scouting_gw.yml`, con la stessa
    forma degli altri (checkbox, testo in italiano comprensibile, es.
    "Riusa le previsioni gia' calcolate per questa giornata (piu' veloce,
    ma non ricalcola con G)"), default SPENTO.

Inoltre:
 - deve LOGGARE in chiaro che il riuso e' spento e quante previsioni sono
   state rifatte per questo motivo (l'utente deve accorgersene dal log,
   non deve andarlo a cercare nel codice);
 - se scopri altri interruttori importanti dello scouting non esposti
   nella UI, ELENCALI nell'esito. Non aggiungerli tutti di tua
   iniziativa: e' l'utente a decidere quali gli servono in mano.

--- REGOLE DI CONSEGNA ---------------------------------------------------

- NESSUN push su main. Committa se vuoi, ma dillo; il push lo fa l'utente.
- Compito 1 e 2 sono MISURA: nessuna modifica alla produzione.
- Se un numero non lo hai misurato, scrivi "non misurato", mai una stima.
- Riporta l'esito IN CODA A QUESTO FILE, non in un file nuovo.
- Se scopri che una cosa scritta in questo brief e' falsa, dillo per
  primo: l'orchestratore preferisce essere smentito subito. E' gia'
  successo oggi due volte, ed e' stato utile entrambe le volte.

--- ESITO (Sonnet esecutore, 09/08/2026 notte, Roma) -------------------

COMPITO 1 -- MISURATO, nessuna modifica al codice. L'utente ha fornito il
cookie di sessione via file locale (Downloads), usato solo in memoria per
il test, MAI scritto nel repo.

Aggiunto a `anyPlayer` dentro SEARCH_QUERY (query esatta, nessuna
riscrittura): `playerGameScores(last: 3) { projection { grade } }`.
Client usato: `_client_ritmato()` (bot_profit), come da brief.

 a) La query e' ACCETTATA, zero errori, su pagina 1 E su pagina 2/3
    (testato pageSize 50, query completa con tutti i campi gia' in
    produzione + il nuovo). Nessun segnale di "depth/complexity" fino
    alla pagina 3. Quindi si': il grade arriva SENZA query aggiuntive.

 b) MA il dato e' inutile per lo scopo dello scouting. Verificato
    aggiungendo anche `anyGame { date }` e `scoreStatus`:
    `playerGameScores(last:N)` e' STRETTAMENTE STORICO -- ordina i
    game-score dal piu' RECENTE (compreso quello in corso oggi, status
    REVIEWING) a ritroso, MAI in avanti. Su un campione di 50 giocatori
    del pool (fixture target football-11-14-aug-2026, finestra
    2026-08-11T14:00Z / 2026-08-14T13:59Z), **0/50** hanno la partita
    piu' recente restituita dentro quella finestra: la piu' recente era
    sempre una partita del 06-09/08 (la giornata CORRENTE/precedente,
    gia' iniziata o finita), mai quella futura che lo scouting sta
    valutando. Conferma sul campo esattamente quello che §8ter gia'
    diceva ("storico, non proiezione per la giornata da giocare") --
    non era un'ipotesi da riverificare, era gia' vera.

 VERDETTO: tecnicamente gratis, ma DA NON USARE cosi' com'e' -- darebbe
 il grade di una partita diversa da quella su cui si sta decidendo
 l'acquisto, silenziosamente sbagliato se qualcuno lo prendesse per
 buono. Nessuna implementazione fatta (come richiesto). Non esiste,
 nei campi provati, una rotta che dia la proiezione FUTURA per un
 giocatore non posseduto: l'unica che Sorare espone per il futuro resta
 `myFilteredBench` (carte possedute), gia' documentato in §8ter come
 irraggiungibile per lo scouting.

COMPITO 2 -- VERIFICATO sul codice, nessuna modifica.
`_prezzo_eur()` (scouting_gw.py:462) legge `lowestPriceAnyCard` dal
payload di `searchPlayers` (stesso oggetto `anyPlayer` di L5/L10/odds
ecc.). Cercato in tutto `scouting_gw.py`: nessuna occorrenza di
`liveSingleSaleOffers` (plurale, query dedicata) ne' di nessun'altra
chiamata GraphQL per prezzi -- solo `liveSingleSaleOffer` (singolare)
dentro lo stesso campo `lowestPriceAnyCard` della query unica.
CONTEGGIO QUERY DEDICATE AI PREZZI IN UNA RUN DI SCOUTING: ZERO.
Conclusione: NESSUNA cache prezzi implementata. Aggiungerne una non
risparmierebbe query (i prezzi arrivano gia' gratis dentro le ~12
chiamate di searchPlayers) e in cambio darebbe prezzi vecchi su una
decisione d'acquisto -- esattamente il rischio che il brief segnalava.

COMPITO 3 -- FATTO, modifica applicata e committata (non pushata).
 a) Nuovo flag `SCOUTING_RIUSA_PREDIZIONI` in scouting_gw.py, default
    '0' (SPENTO). Non tocca `best_five.RIUSA_PREDIZIONI` ne' il suo env
    var `BEST_FIVE_RIUSA_PREDIZIONI`: il generatore di formazioni resta
    con riuso acceso di default come prima. La `bf` che
    `_predizioni_gia_fatte` importa e' un'istanza fresca creata da
    `_import()` (spec_from_file_location per ogni chiamata): spegnere
    il riuso li' non tocca nessun altro import di best_five.
    Verificato che il gate giusto e' proprio qui: il job matrice della
    workflow (`predict`, chiama `test_gk.py`/`test_def.py`/ecc. per
    singolo giocatore) e i `build_consiglio_*.py` NON contengono nessun
    controllo di riuso proprio -- l'unico punto che decide chi va
    ripredetto e' `_predizioni_gia_fatte`/`_scrivi_lavori`, che scrive
    `dati_globali/scouting_lavori.txt` da cui la workflow costruisce la
    matrice. Spegnerlo li' e' sufficiente: a riuso spento TUTTI i
    candidati finiscono in `scouting_lavori.txt` e vengono ricalcolati.
 b) Aggiunto input `riusa_predizioni` (checkbox, default `false`) a
    `.github/workflows/scouting_gw.yml`, con testo "Riusa le previsioni
    gia' calcolate per questa giornata (piu' veloce, ma non ricalcola
    con G)". Passato al job `candidati` come
    `SCOUTING_RIUSA_PREDIZIONI: ${{ inputs.riusa_predizioni && '1' || '0' }}`.
 - Log aggiunto in `_predizioni_gia_fatte`: sempre stampa se il riuso e'
   ACCESO o SPENTO e quante previsioni su quante totali sarebbero state
   riusabili (a riuso spento, dice esplicitamente "vengono RIFATTE per
   questo motivo"). Testato in locale con lista vuota: log corretto,
   default confermato SPENTO (`sg.SCOUTING_RIUSA_PREDIZIONI == False`).

ALTRI INTERRUTTORI NON ESPOSTI NELLA UI DEL WORKFLOW (elencati, non
toccati -- decide l'utente quali gli servono in mano):
 - `SCOUTING_STATI` (default 'starter,regular'): quali playing_status
   considerare titolari nel filtro searchPlayers.
 - `SCOUTING_IDONEI_SU` / `SCOUTING_PARTITE` / `SCOUTING_MINUTI`: i
   parametri del controllo 2-su-3 (oggi la UI ha solo l'on/off
   "screma", non le sue soglie).
 - `SCOUTING_ARENE_PER_EURO` (default acceso): ordina le arene per
   essenze-per-euro invece che per essenze pure.
 - `SCOUTING_PREZZO_IGNOTO` (default 25 EUR): il prezzo assunto per
   candidati senza prezzo noto, penalita' che decide se restano in
   classifica.
 - `SCOUTING_EURO_1000_ESSENZE` (default 2): il cambio essenze->euro
   usato per calcolare le "GW di rientro".

File toccati: `scouting_gw.py`, `.github/workflows/scouting_gw.yml`.
Nessun push su main (regola di consegna). Nessuna modifica alla
produzione del generatore di formazioni.


--- SECONDO GIRO (orchestratore, 09/08/2026 notte) -----------------------
--- LO SCOUTING SEMPLIFICATO + IL GRADE C'E', E' SOLO DA TROVARE ---------

FATTO NUOVO, DETTO DALL'UTENTE E DECISIVO: il grade della GW4 (fixture
che inizia l'11/08) e' GIA' VISIBILE su Sorare ADESSO, sia nella pagina
"Scouting" (la lista filtrabile) sia nella scheda del singolo giocatore.
Quindi il dato ESISTE gia' per giocatori NON posseduti.

CONSEGUENZA SUL COMPITO 1 DEL PRIMO GIRO: quello 0/50 non dimostra che la
lettera non sia raggiungibile. E' un artefatto dell'argomento usato:
`playerGameScores(last: 3)` chiede per definizione le partite PIU'
RECENTI, cioe' guarda indietro. Il dato futuro non e' stato cercato e non
trovato: non poteva comparire. La conclusione "non esiste una rotta per i
non posseduti" NON e' dimostrata e va considerata sospesa.

INDIZIO FORTE, da cui partire: dentro SEARCH_QUERY c'e' gia'
`... on Player { age ownedCardsCount nextClassicFixtureProjectedScore }`.
Quel campo e' il punteggio PROIETTATO per la PROSSIMA fixture: il
concetto giusto, ma il numero. E' molto probabile che accanto esista il
grade (nome tipo `nextClassicFixtureProjectedGrade`, oppure un oggetto
`projection { grade score }` sullo stesso Player). Inoltre `searchPlayers`
E' LA QUERY DELLA PAGINA SCOUTING DI SORARE (§2.2): se quella pagina
mostra la lettera all'utente, il campo e' raggiungibile da li'.

METODO (l'introspezione e' disabilitata, vedi CLAUDE.md): si chiede un
campo plausibile e SI LEGGE IL MESSAGGIO D'ERRORE, che elenca/suggerisce
i campi validi ("Did you mean ..."). E' il modo gia' usato con successo
in questo repo. Prova in quest'ordine, una alla volta, su UNA pagina:
  1. `nextClassicFixtureProjectedGrade` (simmetrico al campo esistente);
  2. `projection { grade score }` sullo stesso `... on Player`;
  3. `playerGameScores` con argomento IN AVANTI invece di `last:`
     (`first:`, o senza argomento) -- e riporta quali argomenti l'errore
     dichiara ammessi;
  4. qualunque nome suggerito dagli errori dei tentativi 1-3.
Riporta il TESTO ESATTO degli errori: e' li' che sta l'informazione.

QUANDO LO TROVI: verifica su 20-30 giocatori che la lettera restituita sia
davvero quella della GW4 e non di una partita passata. Il controllo si fa
chiedendo anche la data/fixture della partita a cui la proiezione si
riferisce, e confrontandola con la finestra della GW4. Se non riesci a
farti dire la partita, DILLO invece di assumere: e' esattamente l'errore
che ha reso ambiguo il primo giro.

--- COSA VUOLE L'UTENTE: UNO SCOUTING SEMPLIFICATO ----------------------

Testuale: "al momento il tool fa anche le formazioni, guadagno per
essenze eccetera; a me interessa semplificarlo e farlo funzionare, poi
sistemiamo il resto".

Per la GW selezionata (adesso la 4) il tool deve rispondere a due
domande, in quest'ordine:

 1. CHI HA STARTER ODDS >= SOGLIA. Default 0.80.
    ATTENZIONE, richiesta esplicita dell'utente: la soglia va PASSATA
    ESPLICITAMENTE, non lasciata implicita, altrimenti il test fallisce.
    Verifica come lo scouting applica oggi `--odds-min` (filtro dentro i
    `refinements` di searchPlayers, oppure filtro dopo aver ricevuto le
    righe?) e scrivilo nell'esito: cambia quante pagine si scaricano.

 2. PER QUEI GIOCATORI, tre informazioni e basta:
      - prezzo minimo (gia' disponibile: `lowestPriceAnyCard`, gratis
        dentro la stessa query);
      - ATTESO, cioe' la predizione che il generatore fa oggi (e' il
        percorso gia' esistente, quello che la UI chiama "far girare il
        modello sui candidati");
      - GRADE, se il passo precedente lo ha reso raggiungibile.

OUTPUT: una tabella minima -- giocatore, ruolo, club, odds, prezzo,
atteso, grade. NIENTE arene, niente essenze/GW, niente "si ripaga in",
niente sezione formazioni.

COME FARLO SENZA ROMPERE NIENTE (vincolo):
 - NON cancellare le sezioni esistenti. Mettile dietro un interruttore
   (es. `--minimal` da riga di comando + checkbox nel workflow, default
   della modalita' semplificata da concordare) in modo che il tool
   completo resti disponibile e riaccendibile.
 - Il resto del comportamento (client ritmato, filtri, leghe senza
   pipeline) non si tocca.

--- CONSEGNA ------------------------------------------------------------
- Esito in coda a QUESTO file.
- Nessun push su main.
- Se il campo grade NON esiste in searchPlayers, dillo con gli errori in
  chiaro: l'utente vede la lettera sul sito, quindi in quel caso la
  domanda diventa "da quale altra query la prende il sito?", e la si
  affronta al giro dopo. Non inventare un ripiego silenzioso.
